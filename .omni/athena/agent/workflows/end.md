# Workflow: End Session

1. Run `/omni:save` (or call the `quicksave` tool) to record final achievements and decisions.
2. Run `/omni:end` to finalize the session and commit logs.
3. Provide a summary of the session to the user.
