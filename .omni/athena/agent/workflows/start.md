# Workflow: Start Session

1. Run `/omni:start` to boot the session (or call the `omni_boot` tool).
2. Sync the Omni Project Context by calling the `opc_sync` tool.
3. Review the Narrative and Structure provided by the startup hook.
4. Check the Project State in `.omni/athena/context/project_state.md`.
