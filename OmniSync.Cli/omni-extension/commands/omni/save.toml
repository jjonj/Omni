description = "Save a checkpoint to the session log"
prompt = "Generate a comprehensive summary of the recent work, capturing all key technical details, architectural decisions, and progress (avoid being overly brief). Then, execute 'python -m athena save \"summary\"' via the Assistant Service, using --bullets for specific atomic items."
