description = "Initialize Omni Assistant workspace"
prompt = "Initialize the Omni Assistant workspace automatically by calling the 'omni_setup' tool with the current project root. This will scaffold all required directories (.omni/athena/, .omni/projectcontext/) and template files via the Assistant Service. After setup, call 'opc_sync' to perform the first Project Context scan."
