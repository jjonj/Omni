description = "Boot the Omni Assistant session"
prompt = "Boot the Omni Assistant session by running 'python -m athena --boot' via the Assistant Service. This will initialize the session log and prime the AI with the core identity and recent context."
