# Tasks


When finishing a task make sure to prefix the line with "✅" and also git commit before moving on to the next task, . Dont wait until the megasession is completed to make these updates.
if told to do a megasession then fix/implement as many issues of the ones below as possible without user intervention. Make sure to git commit and mark as DONE between each one. Start with any tasks that can be validated with a script in D:\SSDProjects\Omni\TestScripts. If strictly necessary you may use AISpeak to ask the user for help/clarifications.

## General
DONE STILL-ISSUE The app is somehow in some kind of fullscreen all the time, hiding the top android bar that shows time and notification icons etc. I only want this in horizontal mode. 
## For the hub

### Hub window

DONE Clear log on startup
DONE add clear log btn

## in OmniSync.Android
### General

### Dashboard

### Remote Control Screen

Stream PC monitor, replacing background of the trackpad leaving the trackpad functioning still, options to toggle monitor, pinch to zoom, (advanced feature, do last)       


### Files & Video Screen

DONE Files screen upper right should have a home button to go to root.
DONE Files screen upper right should have a boomark current folder button
DONE "remote changes detected" popup should allow override

I bookmarked a file and it initially worked but later when i tried to use the bookmark, its just infinitely loading. If i find i t in the file list and open that way it opens fine. Investigate.

DONE Long press option to delete file (with confirmation)

Line numbers dont work right when wrapping lines, it counts a wrapping line as a new one (Disabled word wrap for now when line numbers shown)
Search in txt file next prev/buttons jumping but not jumping to the right place always in file, especially after the last entry it seems to jump to more non existant entries as if it cant overflow the jump offset, maybe has to do with issue above. Also add different color highlighting 

Downloading a video as encrypted currently prompts for the password, is it possible to encrypt without asking for password? isnt that what a public key is for? Only ask for password if no global password is set. Refactor to use public/private key, but how does that work with the password changing? 

Unecnrypted video files play fine but Encrypted videos cannot play. How does playing encrypted videos usually work? decrypt into memory?

The button to open cached files list should have a different icon


DONE Support for displaying image files, needs wide support which includes webm gif etc. Should have slideshow option with random setting and time option. 

DONE Long press folder has both "AI CLI Here" and "CLI Here", only keep "CLI Here" as that works better.
	   

	   
	   
DONE More Text editing fancy features, think like a lite code editor like notepad++ or VS code. be creative. MORE options in burger menu with lots of options
	   
DONE When closing the last open file, go back to last folder. 
DONE Allow swiping sideways to switch between when multiple open files are present
	   
NOT-WORKING Bold/italic/strikethrough options on selected text (We need to somehow hook into androids selected text popup thing and add more options to it)

Not-working Highlight matching brackets/parentheses
Not-working Pinch to zoom (font size change), allow zooming out very far
	   
Improve render markdown like #, ##.., [title](url), use .md rendering like e.g. obsidian
	   
### Browser control screen
	   
	   
Bookmark current tab no longer works

	   
### Notification panel
	   
When clicking notification icon/background it currently expands, it should open app instead. 

### Android Home screen widgets

NOT-WORKING Add support for android launcher widgets, when you add a widget you can choose a quick action (same as the list from notifications)

### Settings screen (accessed by button on dashboard, not bottom panel)

### AI screen

DONE Sending messages to AI from android does not work, hub does not show a long message. (creating new sesion works and the ais messages get sent to android fine)
DONE Closing sessions does not work, it clears locally but remotely the cli does not close.  the number for number of sesions does not change (probably fine given unexpectedly hub didnt close it)
DONE Allow renaming sessions from their PID identifier
DONE Instead of having vertical ... button for session list, use a centralized dropdown, the dropdown always displays the name of the current session but can be pressed to list and change the sessions
DONE Better separation between incoming messages in the speech bubbles, sometimes they append eachother in the same line even though it was separate responses. It seems to separate better when reloading history.

NOT-Working auto scroll down when new messages come in.
DONE AI Screen should be more verbose about showing a status. e.g. "Starting CLI...", "AI Thinking" etc.
DONE Make system messages like errors or "A new version of Gemini CLI is available!" a different color/message type than actual messages from the AI.


### Alarm screen
DONE When alarm pops, force open the omnihub app to open. Do we not have permission from android to open an app automatically? im pretty sure other alarm apps do. (Added explicit startActivity and permissions)
DONE Dismissing alarm from alarm screen does not deactivate a non repeating alarm like expected.
DONE When an alarm is snoozing it should still show the dismiss button on the alarm screen
DONE WHen changing the time of an alarm, it should automatically activate itself


### Smart home integration
DONE Read SmartFeature.txt (implemented ExternalCommandController in Hub)

## Web