temp notes for wartribes 

# Tasks

## Intro

This document contains a number of subtasks.

When finishing a subtask and BEFORE moving on the to the next subtask in here, ALWAYS do the following:
1. Build to check for errors.
2. Move the line to TasksDone.txt
3. git commit (use a txt file called commitmsg.txt for the message, overwrite if exists)
Do not do batch moving or batch committing, do it between each.     
If told to do a megasession then fix/implement as many issues of the ones below as possible without user intervention and move them to TasksDone.txt as they are completed.
If necessary you may use AISpeak to ask the user for help/clarifications.
Only work on subtasks if told to do so or if told explicitly to do a Megasession. Did the user say megasession?
If a task is prefixed [BIGTASK] You should not do it as part of hte megasession, instead when everthing else is done, ask the user if they would like to interactively work on it with you.

## General


## For the hub

The Omnihub window -> Settings -> Hotkeys needs improvements (Completed)


## in OmniSync.Android
### General

### Dashboard


### Remote Control Screen

#### Macro screen


### Files & Video Screen
 

### Browser control screen


### Android Home screen launcher widgets

### Settings screen (accessed by button on dashboard, not bottom panel)



### AI screen



[BIGTASK] Add a special "Manager Session", a CLI session that is always running with some heavy scaffolding around it.
 - First step would be to add a "request_user" command to the omni MCP server, that lets gemini CLI instances request feedback from user. 
 - It will be "woken up" by the scaffolding whenever one of the following happens: request_user command. A Cli instance ends it turn without request_user. 
 - The management cli instance will be given a snippet of recent messages from both the user and the AI and will be asked to condence down what the status of hte client cli instance is and present the status and options to the user. 
 - The management cli will have a sort of queue of things to process
 - The management CLI can resume other client clis by simply sending a "-" character, (which will be interpreted as continue)
 - ???


### Embedded web screen


### Alarm screen

### Books screen

On the landing page that shows "All" books, add a separate section for books and audiobooks that are in progress. 

In the browse book lists, make a long press menu that has "Set Category" (moves the book to the folder), "Remove Progress", "Hide" and "Delete" to  delete from the remote library (also deletes athe offline version if its downlaoded) with a confirmation popup.
In offline tab, allow deleting books offline without confirmation (as we can just redownload). 


For the text book reader
add customizable background and text color. (when hte pdf is text based)
Add invert colors (to automatically let me change a white page black letters to be opposite when the pdf is image based)
Add pinch to zoom in and out, persist zoom level between pages and even when resuming the book later.

Add playback speed options for audiobooks 
Add a timer option to audio books that optionally lets the playback stop after 15/30/60/120 minutes OR after current chapter for multifiles.
 

When playing multi-file audiobooks implement these:
1. show the file name of the current file in the player
2. add next/prev buttons to go between files


Epubs still dont work, it shows a black screen with "Loading EPUB..."


### Settings screen



## Chrome extension

## Omni Web


### Omni web tft

## Chrome extension
