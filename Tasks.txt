# Tasks

## Intro

This document contains a number of subtasks.

When finishing a subtask and BEFORE moving on the to the next subtask in here, ALWAYS do the following:
1. Build to check for errors.
2. Prefix the line in tasks.txt with "✅" 
3. git commit (use a txt file called commitmsg.txt for the message, overwrite if exists)
Do not do batch marking with ✅ or batch committing, do it between each. .

Only work on subtasks if told to do so or if told to do a Megasession.
If told to do a megasession then fix/implement as many issues of the ones below as possible without user intervention. 
If necessary you may use AISpeak to ask the user for help/clarifications.


## General

✅ ✅ The disconnected red color background is too strong, make it darker


## For the hub

### Hub window

## in OmniSync.Android
### General

### Dashboard

✅ Adb log button should filter by com.omni.sync

### Remote Control Screen



Stream PC monitor, replacing background of the trackpad leaving the trackpad functioning still, options to toggle monitor, pinch to zoom, (advanced feature, do last)       

 

### Files & Video Screen

✅ The jump/scroll to cursor when typng out of view is not working right, on the text editor screen, add a temporary panel that twice displays the currently visible line numbers, once accounting for keyboard and once not accounting for android keyboard as well as the current cursor line number, for debugging purposes and then only scroll/jump when typing and the cursor is outside the visible line numbers

✅ On folder view, Recent changes made the bottom bar with bookmarks hover too far above the navigation bar. fix that


it also auto sceolls when placing the cursor but it should not as by definition the cursor place location is in view 

✅ In text editor we want to hook into the select popup to add options like itallic *x* and bold **x**, see here 
In Android, that popup is called the **Selection Action Mode**.
Above works but
✅ 1. the menu is right on top of the selected text, it needs to be below or above (depending on where there is space). 
✅ 2. When attempting to dismiss the menu by  tapping elsewhere it disappears only for 1 frame and then reappears, probably because the previous text is still selected (should also be deselected)


### Browser control screen 


✅ Input controls area takes up too much space vertically, make it 30% smaller. 
✅ Advanced actions  panel can be compressed with less padding/spacing and very slightly thinner buttons, make it like the button panel
✅ Merge the browser buttons below the url bar into the button panel
✅ Make the boomark current tap a small icon to the right of url button like the current paste and go button

### Notification panel
	   
### Android Home screen launcher widgets

Use better widget icons somehow, either let user choose or be smart 


### Settings screen (accessed by button on dashboard, not bottom panel)

### AI screen

✅ make it clear that we are disconnected, disable buttons, but make sure reconnect reenables


✅ Add button to set/toggle text zoom level to 5x (currently text zoom can be set with ctrol scrollwheel so investigate how that works) 

### Alarm screen
✅ Dismissing alarm from notification doesnt disable the alarm and it pops again after snoozze duration. unlike the other dismiss button on alarm screen which works fine.  Why arent these two calling the same function?
