temp notes for wartribes 

# Tasks

## Intro

This document contains a number of subtasks.

When finishing a subtask and BEFORE moving on the to the next subtask in here, ALWAYS do the following:
1. Build to check for errors.
2. Move the line to TasksDone.txt
3. git commit (use a txt file called commitmsg.txt for the message, overwrite if exists)
Do not do batch moving or batch committing, do it between each.     
If told to do a megasession then fix/implement as many issues of the ones below as possible without user intervention and move them to TasksDone.txt as they are completed.
If necessary you may use AISpeak to ask the user for help/clarifications.
Only work on subtasks if told to do so or if told explicitly to do a Megasession. Did the user say megasession?


## General


## For the hub


## in OmniSync.Android
### General

### Dashboard

I cant scroll down anymore so the log area is below the screen

The sleep tracker starts counting when i turn off my pc and go to bed but in the morning it says "User active" and has stopped tracking, despite never connecting to the hub, this might be because the app was force closed by android during the night and on reopening it it lost tracking. Fix this..

## General


### Remote Control Screen

#### Macro screen


### Files & Video Screen

### Browser control screen

### Android Home screen launcher widgets

### Settings screen (accessed by button on dashboard, not bottom panel)

### AI screen

unit test all of the above first 

### Embedded web screen

### Settings screen


## Chrome extension

See if we can find a way for when the chrome extension throws an error, to notify hub about that error and have the hub log it to a new log file CHROME_EXTENSION_ERROR.log in the omni root folder.

See if we can find a way to programatically reload the extension in vivaldi. (C:\Users\crovea\AppData\Local\Vivaldi\Application\vivaldi.exe)

## Omni Web

### Omni web tft

	Show active/partial traits under the must include units and traits drag box like we do with result tiles
	
	Repalce feature to hover and select another unit in results tiles is broken, i see the popup but nothing hgappens when i click other units