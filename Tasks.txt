temp notes for wartribes 

# Tasks

## Intro

This document contains a number of subtasks.

When finishing a subtask and BEFORE moving on the to the next subtask in here, ALWAYS do the following:
1. Build to check for errors.
2. Move the line to TasksDone.txt
3. git commit (use a txt file called commitmsg.txt for the message, overwrite if exists)
Do not do batch moving or batch committing, do it between each.     
If told to do a megasession then fix/implement as many issues of the ones below as possible without user intervention and move them to TasksDone.txt as they are completed.
If necessary you may use AISpeak to ask the user for help/clarifications.
Only work on subtasks if told to do so or if told explicitly to do a Megasession. Did the user say megasession?


## General


## For the hub


## in OmniSync.Android
### General

### Dashboard

## General


### Remote Control Screen

add mic icon to button panel that actives voice dictation 

#### Macro screen

### Files & Video Screen

the context menu when selecting text is currently always showing at the top of the screen, it should intelligently stay near but not overlap selected text 

add mic icon to quick action bar that actives voice dictation at cursor pos

### Browser control screen

if attempting to run any browser command and there is no connection to the browser extension, check if vivaldi.exe is running,if not then send keyboard input win-2 which will start browser 


### Android Home screen launcher widgets

### Settings screen (accessed by button on dashboard, not bottom panel)

### AI screen

add to the list of quick insert messages dropdown: /auth and /conductor:status

add mic icon to input bar that actives voice dictation

### Embedded web screen

### Settings screen

create a new quick action for "Tell PC", this will instantly start a voice dictation with a sound indicating being ready 


## Omni Web

### Omni web tft
