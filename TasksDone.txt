# Completed Tasks

## General

✅ need poc for persistent pcg changes, change 07 diff saved and compared by pcg node using 2d position hash, allow exclusion or modification.
✅ Wake on lan sending while on local network is broken, it used to work. (Added 255.255.255.255 broadcast for better reliability)
✅ Implement Local First, WAN Fallback strategy for Android connection.
✅ Implement Dual-Target WOL (Local Broadcast + WAN Unicast).

## For the hub

### Hub window

✅ The settings page is missing setup for hotkeys, read the existing hotkey management code and ask user to settle anything unclear using aispeak (Added hotkey management to Settings.html and expanded Hub logic to handle more actions)
✅ Add mapping button in settings doesn't work, nothing happens. (Fixed by ensuring UI collection refresh on the UI thread and adding logging)
✅ Allow changing categories of existing entries in Hub settings.
✅ Show the hotkey of existing entries in Hub settings.
✅ Fixed hotkey deletion bug in Hub settings.
✅ Persisted expander state in Hub settings to prevent accidental collapsing.

## in OmniSync.Android

### General

✅ The remote control screen has a fulscreen keyboard feature in horizontal mode, i want that to activate not only on control screens (but i may exclude certain sc reens in the future)
✅ android app attempts to reconnect only once when disconnected. it needs to try to reconnect after 0, 10, 30, 60, 120, 1800 repeating, seconds as well as whenever the app reopens 

### Dashboard

✅ After using wake on lan, Start a reconnecting cycle after 1 minute
✅ Prominently display the time the user has been asleep, guess from the latest time when an alarm was set, the app opened, phone connected to battery or the app has been disconnected from hub and display on dash and notification when not connected in minutes and hours, this needs to persistent between app restarts and needs to detect last disconnect time even with app in background
✅ on app startup it doesn't connect to hub, it like connects and disconnected twice before finally connecting, very odd. add to logging disconnect reason
✅ Add option to fetch hub log and display that instead of activity hub, and option to go back to activity log
✅ i can select one message in the activity log but i can't select multiple, they are like separate elements. i want to be able to select all log, and copy all to clipboard button (simple icon button)
✅ Settings screen (accessed by button on dashboard, not bottom panel) - Added prominent Settings button to Quick Actions card.
✅ I cant scroll down anymore so the log area is below the screen (Refactored to LazyColumn)
✅ The sleep tracker starts counting when i turn off my pc and go to bed but in the morning it says 'User active' and has stopped tracking... (Fixed persistence and accidental resets)

### Remote Control Screen

#### Macro screen

✅ Streaming monitor to this screen queries earlier but was flickeribg. now it doesn't work at all
✅ Add 5 more icon options
✅ Add button on creation screen to test macro as is.
✅ Syntax error highlighting on all lines that doesnt have the cursor
✅ Process macro on hub after saving, first processing step will be to look for pathless run commands...
✅ Ensure run commands support having spaces in the path of the program
✅ Winactivate command should be intelligent about finding the process to activate...
✅ Use normal brackets () instead of {} for special keys
✅ Add more easy insert buttons, including a dropdown for special keys
✅ At the bottom of the help screen add 10 different examples of correct scripts...
✅ some macros are sent to hub entirely and some are managed by android...
✅ analyze android back button behavior...
✅ Stream PC monitor, replacing background of the trackpad...
✅ Streaming pc monitor should have a on/off toggle button (default off)...
✅ streaming desktop not working at all...

### Files & Video Screen

✅ In folder view Use scaling size font for the path at the top left, longer path should use smaller font 
✅ when long pressing folder, add option to copy path to clipboard (Verified in dropdown menu)
✅ Image viewer needs to display the name of the file shown (Verified/Implemented)
✅ When switching back to file screen, restore the last shown sub-screen (Files vs Editor) correctly.

### AI screen

✅ when going to the ai screen, always scroll to bottom (Improved reliability)
✅ Add prominent "Model" button to select Gemini CLI models via CLI command.
✅ change the zoom button to zoom even more (increased from 1.5x to 2.0x)
✅ the automatic merge feature does not work well, seems everything is marked as conflict, run some unit tests
✅ When any ai session is active, add "AI: Add dir to [SessionName]"...
✅ The header at the top showing current path should allow long pressing it...
✅ + button to add files does not work in offline mode...
✅ add logging in activity log for all cache related operations
✅ allow caching a whole folder recursively but with a max size per file...
✅ to settings screen add a way to exclude files and folders from automatic caching recursively...
✅ When we have files edited offline and we connect and see that last saved time don't match cached save time on windows...
✅ if a certain ratio of lines mismatch then fall back to conflicted suffix
✅ on image viewer screen the android back button seems to close app instead of going back...
✅ The jump/scroll to cursor when typng out of view is not working right...
✅ On folder view, Recent changes made the bottom bar with bookmarks hover too far above...
✅ it also auto sceolls when placing the cursor...
✅ On the small menu when long pressing without selecting... add emoji button
✅ Improve markdown rendering by hiding formatting...
✅ plus button to create text file does not work
✅ long press for files and folders should have copy to and move to options...
✅ when a text file is open and i switch screen and then switch back to file screen then the file is now closed...
✅ when i first open a text file and tap to place cursor, it auto scrolls...
✅ When keyboard is up there is a black blank box above the keyboard...
✅ When i have a text for open and then switch to another screen and then back to file screen then file is still open...
✅ the right side scroll bar is not grabbable...
✅ Add a cool CLI hookin feature...
✅ The back button when a file is open is buggy...
✅ The file screen used to have a + button in folder view to create text file...

### Browser control screen

✅ Remove the "Advanced actions" and "Bookmarks" subheaders...
✅ Custom patterns added to the browser cleaning do not persist...
✅ The bookmarks can be reordered with arrow buttons...
✅ The whole screen has some padding on the left and right...
✅ Write a unit test that does a bunch of stuff to the browser in sequence...

### Notification panel

✅ When sleep tracking, show current sleep time in the notification...

### Android Home screen launcher widgets

✅ Use better widget icons somehow...
✅ Add a super cool feature for this please. (Implemented Smart AI Clipboard action)
✅ Added Macros to widget configuration selector.

### Settings screen (accessed by button on dashboard, not bottom panel)

✅ Add settings to configure Cortex web...
✅ add more quick action options...
✅ Add more customization and quick actions.

### AI screen

✅ When i send a message to AI, always scroll to bottom.
✅ When not scrolled to bottom of history i experience the scroll amount jumping...
✅ merge the + and foldericon buttons.
✅ sessions are named according to the first message sent to ai...
✅ Code functionality of Ai screen recently broke...
✅ the new folder icon button when using s bookmark does not work right
✅ when rendering a filename... turn that into a link
✅ auto scroll to bottom of chat when a new message comes in...
✅ replace reset all sessions button with a reload all sessions button.
✅ sometimes when sending a message to AI, the chat window shows my message to the AI as expected but it also shows my message a second time...
✅ the browse option in the dropdown will always call /dir add...
✅ add a button to Focus, which will have windows focus the windows bringing it to front
✅ when sending input keys like esc or enter or yolo make sure to focus the window first
✅ clicking file links in chat jumps to file screen as expected but it does not find and open the file...
✅ when sending a message, it doesn't scroll to bottom
✅ auto scroll to bottom can sometimes be hard to activate...
✅ when a chat gets a dialog that needs users choice, play a subtle sound and highlight...
✅ On anddroid i cant see if a session has stopped or if it's waiting for server response...
✅ the focus button works if the window is behind other windows but not if the window is minimized.

### Alarm screen

✅ support activating a macro when alarm begins ringing OR when alarm is dismissed

### Omni web screen (Embedded Web Server)

✅ Does not work, simply black
✅ Add a "more" button that switched the button panel to exactly the same panel...

## Chrome extension

✅ Add to extension right click menu option to clean tabs from within chrome.
✅ Fixed icon in manifest.json.
✅ See if we can find a way for when the chrome extension throws an error, to notify hub about that error...
✅ See if we can find a way to programatically reload the extension in vivaldi. (Added Ctrl+Alt+R hotkey)

## Omni Web

### Omni web tft

✅ Top Units Suggestion feature in TFT results
✅ Comp Evolution Explorer feature in TFT results
✅ (COMPLETED) Workaround for mismatched function response parts API error in Gemini CLI
✅ Cannot create solutions if annie is in must include for some reason...
✅ On page load i get tft_evolution_test.js?v=2026.01.17.00.12:12 Uncaught ReferenceError: require is not defined
✅ In smart sort mode, units that are already in the active input field should be hidden.
✅ Change default active levels from 6 and 8 to just 7
✅ Tibbers is missing from unit list
✅ add hotkey for toggling 'smart sort'
✅ Show active/partial traits under the must include units and traits drag box like we do with result tiles
✅ Repalce feature to hover and select another unit in results tiles is broken
✅ Clear log file on startup.
✅ Allow seeing git log/commits for folders that are repos, browsing history and seeing the changes of clicked commits.
✅ Implemented 'E' (Emblem) and 'T' (Must-include Trait) hotkey prefixes for Active Mode.

## Megasession Jan 21, 2026

### Hub
? Hub Single Instance: Implemented via Mutex and SignalR-based window restoration to prevent multiple instances and port bind errors.

### Android General
? Navigation Stability: Increased touchSlop multiplier to 6.0f to reduce accidental swipes.
? Back Button Optimization: Back button now correctly dismisses the keyboard without triggering screen navigation.

### Android Files
? Folder View Cleanup: Removed unused voice recognition components and icons from folder browsing.

### Android Browser
? Title-Based Cleanup: Added support for "title:" prefix in cleanup patterns (e.g., ChatGPT, Messenger).

### Android AI
? Interleaved Diffs: "replace" and "edit" tool calls now render readable line-by-line (+/-) diffs using greedy matching.
? Scroll to Bottom: Fixed auto-scroll reliability in AI chat window.
? Clear Command: Added history reload after clearAiMessages to ensure synchronization.
? Robust Testing: Updated roundtrip and multi-instance tests with target_pid filtering.

### Android Alarms
? Custom Dismiss Text: Alarm 2 now supports a custom text message shown during the dismiss phase.

### Hub Startup Routines
? QuickActionService: Framework for once-per-day maintenance tasks (e.g., daily tab cleanup).

### Web UI
? Quick Nav: Restricted site selector dropdown to WebServerScreen context and fixed style leaks.
