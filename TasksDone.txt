# Completed Tasks

## General

✅ need poc for persistent pcg changes, change 07 diff saved and compared by pcg node using 2d position hash, allow exclusion or modification.
✅ Wake on lan sending while on local network is broken, it used to work. (Added 255.255.255.255 broadcast for better reliability)
✅ Implement Local First, WAN Fallback strategy for Android connection.
✅ Implement Dual-Target WOL (Local Broadcast + WAN Unicast).

## For the hub

### Hub window

✅ The settings page is missing setup for hotkeys, read the existing hotkey management code and ask user to settle anything unclear using aispeak (Added hotkey management to Settings.html and expanded Hub logic to handle more actions)
✅ Add mapping button in settings doesn't work, nothing happens. (Fixed by ensuring UI collection refresh on the UI thread and adding logging)

## in OmniSync.Android

### General

### Dashboard

✅ After using wake on lan, Start a reconnecting cycle after 1 minute
✅ Prominently display the time the user has been asleep, guess from the latest time when an alarm was set, the app opened, phone connected to battery or the app has been disconnected from hub and display on dash and notification when not connected in minutes and hours, this needs to persistent between app restarts and needs to detect last disconnect time even with app in background
✅ on app startup it doesn't connect to hub, it like connects and disconnected twice before finally connecting, very odd. add to logging disconnect reason
✅ Add option to fetch hub log and display that instead of activity hub, and option to go back to activity log
✅ i can select one message in the activity log but i can't select multiple, they are like separate elements. i want to be able to select all log, and copy all to clipboard button (simple icon button)
✅ Settings screen (accessed by button on dashboard, not bottom panel) - Added prominent Settings button to Quick Actions card.

### Remote Control Screen

#### Macro screen

✅ Streaming monitor to this screen queries earlier but was flickeribg. now it doesn't work at all
✅ Add 5 more icon options
✅ Add button on creation screen to test macro as is.
✅ Syntax error highlighting on all lines that doesnt have the cursor
✅ Process macro on hub after saving, first processing step will be to look for pathless run commands, e.g. "run truecrypt.exe", first check if truecrypt.exe exists as a mapping in hub settings. If not, search the whole system for it, find it at "E:\Program Files\TrueCrypt\TrueCrypt.exe" and send a message back to app and app will update the macro automatically to replace truecrypt.exe with E:\Program Files\TrueCrypt\TrueCrypt.exe
✅ Ensure run commands support having spaces in the path of the program
✅ Winactivate command should be intelligent about finding the process to activate, either by executable name or partial string of window should work.
✅ Add windows key to special key and tab
✅ Use normal brackets () instead of {} for special keys
✅ Add more easy insert buttons (e.g. the sleep run exe etc), including a dropdown for special keys
✅ At the bottom of the help screen add 10 different examples of correct scripts, and also use them as templates that can be inserted in the current edit window.
✅ some macros are sent to hub entirely and some are managed by android and sends commands one by one, display which side manages the macro on edit screen
✅ analyze android back button behavior, does it return as user would expect? (Improved back button handling in FilesScreen and MacroManagerScreen)

✅ Stream PC monitor, replacing background of the trackpad leaving the trackpad functioning still, options to toggle monitor, pinch to zoom, (advanced feature, do last)
✅ Streaming pc monitor should have a on/off toggle button (default off) on page2 of button panel, and to settings for resolutioni and framerate. Default to full resolution 10fps
✅ streaming desktop not working at all, the initial implementation of it worked but was flickering (Fixed flickering and added logging to diagnose 'not working')

### Files & Video Screen

✅ when a file is open the navigation bar gets hidden, change that 
✅ the automatic merge feature does not work well, seems everything is marked as conflict, run some unit tests
✅ When any ai session is active, add "AI: Add dir to [SessionName]"  option to long press folder which will send "/dir add X" to the active session
✅ The header at the top showing current path should allow long pressing it (to give folder context actions)
✅ + button to add files does not work in offline mode, it should be able to create a file that's cached from birth and will sync when connected
✅ add logging in activity log for all cache related operations
✅ allow caching a whole folder recursively but with a max size per file, files above that size are replaced by empty myfile.mp4.fake files. when a folder is cached this way do not list every single subitem in the cached list that already exists, just the selected folder with bold foldername. ask user for clarification. add max file size to settings
✅ to settings screen add a way to exclude files and folders from automatic caching recursively, allowing patterns. e.g. "G:/*", "*porn*". This prevents automatic caching when visiting a folder and when using the above cache whole folder feature but does not prevent visiting those places manually when connected
✅ When we have files edited offline and we connect and see that last saved time don't match cached save time on windows and we save as conflicted.txt then first try to resolve with a simple merge strategy going line by line and comparing. leave some tags like [MERGED] when doing this and be careful not to delete anything, better keep both versions of lines
✅ if a certain ratio of lines mismatch then fall back to conflicted suffix
✅ on image viewer screen the android back button seems to close app instead of going back to the containing folder
✅ The jump/scroll to cursor when typng out of view is not working right, on the text editor screen, add a temporary panel that twice displays the currently visible line numbers, once accounting for keyboard and once not accounting for android keyboard as well as the current cursor line number, for debugging purposes and then only scroll/jump when typing and the cursor is outside the visible line numbers
✅ On folder view, Recent changes made the bottom bar with bookmarks hover too far above the navigation bar. fix that
✅ it also auto sceolls when placing the cursor but it should not as by definition the cursor place location is in view
✅ On the small menu when long pressing without selecting that has paste, select all bold and itallic, add a button for adding common emoji like ✅ etc, make it a looong dropdown
✅ Improve markdown rendering by hiding formatting, e.g. "###" and the * symbols in **bold text** similar to how the obsidian app does it. Those symbols are only visible when the cursor is on the line that contain them.
✅ plus button to create text file does not work (Fixed by correctly adding new file to open files list in ViewModel)
✅ long press for files and folders should have copy to and move to options, reuse browse popup from Ai screen
✅ when a text file is open and i switch screen and then switch back to file screen then the file is now closed and i see folders. don't auto close file like that. (Fixed by removing auto-loadDirectory on FilesScreen init and preserving ViewModel state)
✅ when i first open a text file and tap to place cursor, it auto scrolls, but it should not do this because the place i tappped is by definition in view. auto scrolling is for when typing while out of view of cursor.
✅ When keyboard is up there is a black blank box above the keyboard with the height of the navigation bar. this started after i requested that the navigation bar not be hidden in text editor.
✅ When i have a text for open and then switch to another screen and then back to file screen then file is still open which is good but it has reset the scroll distance and cursor position (Fixed by ensuring scrollRestored flag and proper restoration in TextEditorScreen)
✅ the right side scroll bar is not grabbable not does it accurately display current scroll level, same on file screen (Implemented custom VerticalScrollbar in SharedComponents and applied to both screens)
✅ Add a cool CLI hookin feature, where i can be editing a text file and then have an AI asssistant help me (it works by opening a new CLI session like the AI screen does and will in a smart way send the text of the file to the AI and let the AI edit/insert into the file). Make it really cool and helpful.
✅ The back button when a file is open is buggy, it makes likst a swipe animation to go back to folder view but only makes it half way before stopping. Removed jumpy animations by using Overlay strategy in MainActivity.
✅ The file screen used to have a + button in folder view to create text file, that was apparently removed for no reason. Added FAB for file creation.

### Browser control screen

✅ Remove the "Advanced actions" and "Bookmarks" subheaders, its obvious.
✅ Custom patterns added to the browser cleaning do not persist across reinstalls of app like other settings do. Same goes for bookmarks it seems.
✅ The bookmarks can be reordered with arrow buttons, instead make that a draggable marker so i can drag to reorder.
✅ The whole screen has some padding on the left and right and top and bottom edges wasting pixels, please reduce that padding to just 2 pixels or so. Other screens waste less space.
✅ Write a unit test that does a bunch of stuff to the browser in sequence (without messing with existing non-cleanuppable tabs) and use it to verify browser control works.

### Notification panel

✅ When sleep tracking, show current sleep time in the notification instead of quick actions, but make it a button such that if tapped the buttons come right back

### Android Home screen launcher widgets

✅ Use better widget icons somehow, either let user choose or be smart
✅ Add a super cool feature for this please. (Implemented Smart AI Clipboard action)
✅ Added Macros to widget configuration selector.

### Settings screen (accessed by button on dashboard, not bottom panel)

✅ Add settings to configure Cortex web, need to be able to adjust wake time (currently hardcoded to reset to 7am adjustable for the day but not enough) and block definitions
✅ add more quick action options, including but not limited to, set alarm 8h45m, browser functionality like send youtube history video to chrome etc and double check at can use macros as quick actions
✅ Add more customization and quick actions. (Added Quick Actions card at top and new streaming/global config options)

### AI screen

✅ add a new button that let's me /dir addmypath with a button press and then navigating the file system in a popup to select the folder to add
✅ on all other screens i can swipe left and right to navigate screens but i can't from ai
✅ When not scrolled to bottom of history i experience the scroll amount jumping when new messages are added at the bottom, i dont want it to move what I'm looking at. it would only auto scroll if I'm scrolled to bottom already or if i send a message myself
✅ merge the + and foldericon buttons. when pasting prompt user with the folder bookmarks list and add browse option that opens a popup
✅ sessions are named according to the first message sent to ai with fallback to session id. change that to primarily use the workspace folder name (not full path), append with number if multiple with same workspaces
✅ Code functionality of Ai screen recently broke, i cant send messages to sessions anymore (Fixed by correctly handling nullable sessionId in SignalR calls)
✅ the new folder icon button when using s bookmark does not work right (Improved workspace menu behavior and added arrow sub-buttons for /dir add)
✅ when rendering a filename, i think it uses this format `my.file` turn that into a link that i can click and it will find the file in the workspace of the ai session and item it in file view
✅ auto scroll to bottom of chat when a new message comes in and the current scroll is already at bottom. pretty sure almost all other chat windows in other apps work like that (Implemented in LaunchedEffect in AiChatScreen)
✅ replace reset all sessions button with a reload all sessions button. this feature indicates the user was experiencing issues so it should create a detailed report log file in all caps in root of omni containing list of old displayed sessions on android AND what sessions the hub knows about and compare, their name id active time etc, then analyze the status of those old sessions, do they still live? why were they not cleared automatically? then reload by force checking for active sessions, log what is now detected etc (Implemented ReloadAiSessions in Hub and Android)
✅ sometimes when sending a message to AI, the chat window shows my message to the AI as expected but it also shows my message a second time, but as if the AI sent it to me. this is almost certainly caused by the cli displaying my message and informing the hub that this new message is displayed and the hub misinterprets this as it being a message from the AI instead of the users message that should be ignored because it's already displayed on android when sent (Fixed by using _sentPrompts in Hub and improved filtering)
✅ the browse option in the dropdown will always call /dir add, should be possible to choose wether we want to dir add or start new session there (Added browse options for both to dropdown)
✅ add a button to Focus, which will have windows focus the windows bringing it to front (Added Focus action to TopAppBar)
✅ when sending input keys like esc or enter or yolo make sure to focus the windowbfirst so the right chat receives it (Added FocusSessionAsync calls before sending keys in RpcApiHub)
✅ clicking file links in chat jumps to file screen as expected but it does not find and open the file, instead it seems to try to find a folder with that name and errors (Fixed with AI_FILE: prefix and search logic in FilesViewModel)
✅ add a button that opens a dropdown with preset messages and commands to put into the input text box, the user can persistently add more by entering text into the input box and then long pressing this new button. start with these default 4 options /model /conductor:newTrack /conductor:implement Please run any final scripts and commit all changes (Implemented Presets button with long-press save and Hub-side persistence)
✅ adding a message while the ai is busy will in the cli window queue the message but in android it looks like it got instantly sent, can you find a way to figure out when it's queued and then displsy it differently (Implemented QUEUED status in Hub and Android with 'isQueued' flag on AiMessage)
✅ allow me to select and copy text in the chat (Enabled SelectionContainer in ChatBubble)
✅ support displaying and interacting with AI choice dialogs on Android.

### Alarm screen

✅ support activating a macro when alarm begins ringing OR when alarm is dismissed

### Omni web screen (Embedded Web Server)

✅ Does not work, simply black (Fixed by improving WebView settings and adding diagnostic logging)
✅ Add a "more" button that switched the button panel to exactly the same panel as in control screen AND shows a mini trackpad like browser control. Only difference is that there's a back button somewhere (Implemented 'More Mode' with controls and mini trackpad)

## Omni Web

### Omni web tft

✅ Top Units Suggestion feature in TFT results - Implemented next unit suggestions with score boost and trait impact tooltips.
✅ Add mic icon to button panel in Remote Control Screen that activates voice dictation.
✅ Files & Video Screen: The context menu when selecting text is now intelligently positioned to stay near but not overlap selected text.
✅ Add mic icon to quick action bar in Files & Video Screen that activates voice dictation (injects at cursor if file open).
✅ Browser control screen: If attempting to run any browser command and there is no connection to the browser extension, check if vivaldi.exe is running, if not then send keyboard input win-2 which will start browser.
✅ AI screen quick insert messages dropdown: added /auth and /conductor:status.
✅ Add mic icon to AI screen input bar that activates voice dictation.
✅ Settings screen: Created a new quick action for "Tell PC", which starts voice dictation with a blip sound on the Hub, launches a new AI session in the configured workspace (default B:\GDrive\Tools), and prefixes the message with a configurable system context.
✅ Hub Settings: Added "AI / Tell PC" configuration tab to manage Tell PC workspace, system context, and sound options.