fix(android): Correct variable declaration order and syntax errors in screens

- Fixed `AiChatScreen.kt` build error by moving `listState` and `filteredMessages` before their use in `LaunchedEffect`.
- Fixed `FilesScreen.kt` build error by cleaning up a catastrophic paste error in the `DropdownMenu`.
- Verified build and deployment to device.
