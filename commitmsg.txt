feat(android): Robust reconnection and global horizontal keyboard

- Implement exponential-ish backoff retry schedule for SignalR reconnection: 0, 10, 30, 60, 120, 1800s.
- Extract CustomKeyboard to a shared component for global reuse.
- Enable landscape orientation globally (except Dashboard/Settings) to show horizontal keyboard overlay.
- Fix various Kotlin syntax and import errors related to the refactoring.
- Add unit tests for reconnection delay logic.
