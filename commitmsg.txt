feat(tft): show active/partial traits under Must Include zone

- Extracted renderTraitsSummary into a reusable function.
- Integrated renderTraitsSummary into renderSelectionZones to show real-time trait counts for the Must Include units.
- This improves parity between selection zones and result cards.
