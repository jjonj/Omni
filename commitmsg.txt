feat(android): Fix and improve 'Tell PC' feature

- Implemented centralized voice recognition in MainActivity triggered by 'isTriggeringTellPc' flow.
- Recognized text is sent directly to the Hub's active AI session.
- Removed automatic navigation to AI Chat screen when triggering Tell PC.
- Removed confirmation blip sound from Hub's TriggerTellPc.
- Added developer reminders to keep WPF and Web UIs in sync.