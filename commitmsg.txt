fix(android): make Dashboard screen scrollable

- Refactored DashboardScreen to use a single LazyColumn for all sections (Connection, Sleep, Quick Actions, Logs).
- This ensures the log area is accessible even when cards above it take up significant screen space.
