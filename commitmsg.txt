fix(tft): resolve unit replacement feature failure in result cards

- Added container.innerHTML = '' to the top of renderResults to ensure UI refreshes correctly instead of appending.
- Updated replacement onclick handler to re-calculate trait counts when a unit is swapped, ensuring accurate trait summary display.