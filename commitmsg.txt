feat(hub): Improve Hotkey Settings UI and fix deletion

- Allow inline changing of hotkey categories via ComboBox.
- Use ReadOnly TextBox for existing hotkey display for better visibility.
- Fix deletion bug by using RemoveHotkeyByAction instead of RemoveHotkey.
- Persist expander state across UI refreshes to prevent accidental collapsing.
- Implement INotifyPropertyChanged for HotkeyConfig to support real-time updates.
- Update build and run scripts to handle elevated kill of Hub process.